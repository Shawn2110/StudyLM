# Used only at compile time when regenerating the sqlx offline cache.
# Run from src-tauri/:
#   cp .env.example .env
#   cargo sqlx database create
#   cargo sqlx migrate run
#   cargo sqlx prepare
# Then commit any new files under .sqlx/.
DATABASE_URL=sqlite:./tmp.db
